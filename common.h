#ifndef clox_common_h //ifndef is "if not defined" i.e, if some macro is not defined it loads this header file , it is already defined it keeps and goes to endif
#define clox_common_h


// common types we will use in this interpreter

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

#define DEBUG_PRINT_CODE
#define DEBUG_TRACE_EXECUTION

#endif